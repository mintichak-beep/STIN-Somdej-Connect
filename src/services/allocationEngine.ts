interface Room {
  id: string;
  capacity: number;
  occupied: number;
}

export const allocationEngine = {
  allocateRooms(students: any[], rooms: Room[]): any[] {
    // Logic for automatic room allocation based on rules
    return [];
  },
};
